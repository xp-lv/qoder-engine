{
  "result": {
    "verdict": "confirmed",
    "summary": "已根据需求审计者反馈完成第二轮修正（5项问题全部解决）。AUDIT-01（高）：删除不存在的「代码生成者」角色引用，统一回退目标为前端实现者/后端实现者（根据测试失败维度定向回退）；AUDIT-02（中）：统一 DATABASE_URL 为 file:./dev.db，消除 test.db 与 dev.db 的矛盾；AUDIT-03（中）：补充 local 部署模式验收标准（LOCAL-DEPLOY-1~6），使 local+docker 双模式均有可证伪验收标准；AUDIT-04（低）：统一 DAG 中「校验者」命名为「合并校验者」；AUDIT-05（低）：修正 L3 层将部署文档管理者从「审阅角色」更正为「参考消费角色」。目标系统为 lxp-eng-planning（吕小鹏精力规划系统），新增 F1 测试执行者（P0）和 F2 部署执行者（P1）补齐「测试→部署」闭环，F3 交互式需求澄清（P2）暂不实现。改造类型为新增角色 + 调整编排，不删除现有角色。"
  },

  "需求文档主体": {

    "一、目标": {
      "核心定义": "为 lxp-eng-planning 引擎新增测试执行与自动部署两类自动化能力，补齐「代码生成 → 静态校验 → 运行时测试 → 部署文档 → 终审 → 自动部署」的全链路闭环，消除运行时 bug 逃逸和人工部署瓶颈。",
      "价值主张": "使系统能在沙箱中实际运行代码验证编译和功能可用性，并在终审通过后自动执行部署，实现从需求到上线的全自动交付。",
      "部署形态": "本地沙箱测试 + 多模式部署，部署目标支持 local / docker / remote 三种模式（首期实现 local + docker）。",
      "目标APP路径": "/Users/xiaopenglv/ai-agents/多角色配合系统/apps/lxp-eng-planning/",
      "改造轮次": "第三轮（基于第二轮 21 角色前后端分离架构 + 前端预览闭环基线的增量优化）"
    },

    "二、核心能力": {
      "说明": "以下为核心能力清单，每项标注功能编号（F#）与优先级（P0 最高 / P1 中 / P2 低）。",
      "能力清单": [
        {
          "编号": "F1",
          "名称": "测试执行能力（测试执行者角色）",
          "优先级": "P0",
          "描述": "在合并校验者静态审查通过后，于本地沙箱中实际运行代码，依次执行 7 个维度的测试：依赖安装（npm install）、数据库迁移（npx prisma migrate dev）、TypeScript 编译（npx tsc --noEmit）、前端构建（npm run build）、后端启动 + Health 检查、核心 API 冒烟测试（CRUD）、鉴权验证（401）。",
          "约束条件": [
            "需要 Bash 工具执行 npm / npx / curl 命令",
            "测试在本地沙箱中运行，不需要远程部署环境",
            "每次执行使用临时目录或清理已有 node_modules，确保干净状态",
            "启动测试服务后必须关闭（kill process），不残留进程",
            "环境变量通过命令行注入：AUTH_TOKEN=test-token DATABASE_URL=file:./dev.db PORT=3999"
          ]
        },
        {
          "编号": "F2",
          "名称": "自动部署能力（部署执行者角色）",
          "优先级": "P1",
          "描述": "在综合裁决者终审通过后，自动执行部署流程。根据用户选择的部署目标模式执行不同流程：docker 模式（构建 Docker 镜像、docker compose up -d、健康检查、端到端验证）或 local 模式（npm run build、node 启动服务、健康检查、端到端验证）。",
          "约束条件": [
            "部署目标通过 awaiting_confirmation 收集（deploy_target: local/docker/remote, port, auth_token）",
            "confirm: manual（需用户确认部署目标）",
            "为终态角色，失败则 BLOCKING 由用户介入，无循环回退",
            "首期实现 local + docker 模式，remote 暂不实现"
          ]
        },
        {
          "编号": "F3",
          "名称": "交互式需求澄清（引擎级增强）",
          "优先级": "P2",
          "描述": "允许角色在执行过程中通过特定机制向用户提问，实现「执行中 → 发现歧义 → 产出待澄清问题 → 引擎暂停 → 用户回答 → 继续执行」的交互式流程。",
          "约束条件": [
            "建议暂不实现（方案 C），现有 awaiting_confirmation 机制已能覆盖大部分场景",
            "如未来实现，候选方案：A. 嵌套 awaiting_confirmation（引擎层面改动）或 B. 角色拆分（仅 app.yaml 改动）"
          ]
        }
      ]
    },

    "三、角色清单": {
      "说明": "以下为本次改造新增的角色定义。角色名、类型、职责、producer 入口标识齐全。现有 21 角色不变，此处仅列新增角色。",
      "新增角色": [
        {
          "角色名": "测试执行者",
          "角色编号": "R10",
          "角色类型": "standard（执行型，非 producer）",
          "确认方式": "auto（自动确认，无需人工）",
          "producer入口标识": "否",
          "DAG位置": "合并校验者(confirmed) → 测试执行者 → 部署文档管理者",
          "职责": "在沙箱中实际运行代码，验证编译通过和核心功能可用。执行 7 个维度测试：依赖安装、数据库迁移、TypeScript 编译、前端构建、后端启动+Health、核心API冒烟、鉴权验证。",
          "输入文件": [
            "前端代码: outputs/src/frontend/",
            "后端代码: outputs/src/backend/",
            "Prisma Schema: outputs/prisma/schema.prisma",
            "docker-compose.yml: outputs/docker-compose.yml",
            "校验报告: process/outputs/校验报告.json"
          ],
          "产出物": [
            "测试报告: outputs/测试报告.json, type=process"
          ],
          "循环回退": "测试失败(verdict=BLOCKING) → 回退至前端实现者/后端实现者（根据测试失败维度定向回退，max_executions: 3）"
        },
        {
          "角色名": "部署执行者",
          "角色编号": "R11",
          "角色类型": "standard",
          "确认方式": "manual（需用户确认部署目标）",
          "producer入口标识": "否",
          "DAG位置": "综合裁决者(confirmed) → 部署执行者 → 终态",
          "职责": "在终审通过后自动执行部署：根据 deploy_target 选择 docker 模式（构建镜像、compose 启动）或 local 模式（构建项目、node 启动），均执行健康检查与端到端验证。通过 awaiting_confirmation 收集部署目标信息。",
          "输入文件": [
            "前端代码: outputs/src/frontend/",
            "后端代码: outputs/src/backend/",
            "部署配置: outputs/docker-compose.yml",
            "最终裁决书: outputs/最终裁决书.md"
          ],
          "部署目标配置": {
            "deploy_target": "local | docker | remote",
            "port": 3456,
            "auth_token": "用户提供的 token",
            "docker_compose_path": "outputs/docker-compose.yml"
          },
          "产出物": [
            "部署报告: outputs/部署报告.json, type=deliverable"
          ],
          "循环回退": "无（终态角色，失败则 BLOCKING 由用户介入）"
        }
      ],
      "现有角色总数": 21,
      "改造后角色总数": 23
    },

    "四、协作流程": {
      "说明": "协作流程描述 DAG 拓扑（主链 + 对抗 + 迭代循环），标注流程类型（线性 / 并行 / 迭代）。",

      "流程类型标注": {
        "主链": "线性（含并行扇出与同步汇入）",
        "测试执行者回退": "迭代循环（max_executions: 3）",
        "部署执行者": "线性终态（无回退）",
        "producer自校验": "迭代循环（不设 max_executions，局部回退）"
      },

      "DAG变更概要": {
        "新增步骤": [
          "合并校验者(confirmed) → 测试执行者 [新增]",
          "测试执行者(confirmed) → 部署文档管理者",
          "测试执行者(BLOCKING) → 前端实现者/后端实现者 [max_executions: 3，根据失败维度定向回退]",
          "综合裁决者(confirmed) → 部署执行者 [新增]",
          "部署执行者(confirmed) → 终态"
        ]
      },

      "修改后完整DAG拓扑": [
        "R1 需求接收者（producer, manual）",
        "  → R1-validate 需求接收者（校验）（auto）",
        "  → R2 需求审计者（auto）",
        "    challenged → R1（max_executions: 3）",
        "  → R2-分流 需求分流者（auto）",
        "  → [前端文档管理者, 后端文档管理者]（并行扇出）",
        "  → 交叉审核者（同步汇入·auto）",
        "    FRONTEND_DOC_DEFECT → 前端文档管理者（max:3）",
        "    BACKEND_DOC_DEFECT → 后端文档管理者（max:3）",
        "  → [交互逻辑设计师, 美学布局设计师, 接口翻译师, 后端架构设计师, 数据模型设计师]（并行扇出·5角色）",
        "  → 接口翻译师 → API契约设计师（显式依赖）",
        "  → 前端实现者（producer, auto）→ 前端实现者（校验）",
        "  → 预览部署者（manual）→ 前端美学师（manual）",
        "    needs_revision → [交互逻辑设计师, 美学布局设计师]（max:3）",
        "  → 后端实现者（producer, auto）→ 后端实现者（校验）→ 后端启动验证者（auto）",
        "  → [前端美学师, 后端启动验证者] → 全栈联调验证者（同步汇入）",
        "  → 合并校验者（auto）",
        "    FRONTEND_BLOCKING → 前端实现者（max:5）",
        "    BACKEND_BLOCKING → 后端实现者（max:3）",
        "  → R10 测试执行者（auto）              [新增·P0]",
        "    BLOCKING → 前端实现者/后端实现者（max_executions: 3，根据失败维度定向回退）",
        "  → 部署文档管理者（auto）",
        "  → 综合裁决者（manual）",
        "    challenged → 交叉审核者（max:2）",
        "    deploy_doc_defect → 部署文档管理者（max:2）",
        "  → R11 部署执行者（manual）            [新增·P1]",
        "  → 终态（completed）"
      ],

      "迭代循环规则": {
        "测试执行者回退循环": {
          "触发条件": "verdict == BLOCKING（任一测试维度失败）",
          "回退目标": "前端实现者 / 后端实现者（代码 producer 节点，根据测试失败维度定向回退：前端相关测试失败→前端实现者，后端相关测试失败→后端实现者）",
          "max_executions": 3,
          "说明": "测试失败时回退至对应的代码 producer 修复代码，修复后重新走合并校验→测试链路，最多重试 3 次"
        },
        "部署执行者无回退": {
          "触发条件": "部署失败（verdict = deploy_failed）",
          "处理方式": "BLOCKING，由用户介入处理，不设自动回退循环",
          "说明": "部署执行者为终态角色，部署失败属于环境/配置问题，需人工介入"
        },
        "producer自校验loop": {
          "说明": "局部回退不设 max_executions（需求接收者/前端实现者/后端实现者的自校验 loop）"
        }
      }
    },

    "五、对抗性要求": {
      "说明": "对抗性要求明确各层对抗角色、对抗维度、verdict 定义。本次改造不改变现有三层对抗治理结构，测试执行者和部署执行者不属于对抗角色，但测试执行者构成代码层的运行时验证对抗。",
      "现有三层对抗（不变）": [
        {
          "对抗层": "文档层对抗",
          "对抗角色": "交叉审核者",
          "对抗维度": "前后端文档一致性、接口契约匹配度、业务逻辑完整性",
          "verdict定义": "confirmed / FRONTEND_DOC_DEFECT / BACKEND_DOC_DEFECT / BOTH_DOC_DEFECT"
        },
        {
          "对抗层": "代码层对抗（静态）",
          "对抗角色": "合并校验者",
          "对抗维度": "前后端代码质量、接口实现一致性、编译可过性",
          "verdict定义": "confirmed / FRONTEND_BLOCKING / BACKEND_BLOCKING"
        },
        {
          "对抗层": "全局对抗",
          "对抗角色": "综合裁决者",
          "对抗维度": "全局质量裁决、文档与代码一致性、部署文档完备性",
          "verdict定义": "confirmed / challenged / deploy_doc_defect"
        }
      ],
      "新增运行时验证（非正式对抗层，但构成验证关卡）": {
        "角色": "测试执行者",
        "验证维度": "运行时行为：依赖安装、数据库迁移、编译、构建、服务启动、API冒烟、鉴权",
        "verdict定义": "confirmed（7项全部通过）/ BLOCKING（任一项失败）",
        "与对抗层关系": "位于合并校验者（静态对抗）之后、部署文档管理者之前，作为代码层的运行时补充验证关卡"
      }
    },

    "六、验收标准": {
      "说明": "以下每条验收标准均可证伪，包含编号、标准描述、验证方式、判定标准、期望值。部署验收标准按 docker 模式与 local 模式分别定义。",
      "测试执行者验收标准": [
        {
          "编号": "TEST-1",
          "标准": "测试执行者能执行 npm install 并报告成功/失败",
          "验证方式": "检查 outputs/测试报告.json 中 details 数组里 test='前端/后端 npm install' 条目的 status 字段",
          "判定标准": "status == 'pass' 表示成功，status == 'fail' 表示失败",
          "期望值": "exit code 0，status = pass"
        },
        {
          "编号": "TEST-2",
          "标准": "测试执行者能执行 npx prisma migrate dev 并报告迁移结果",
          "验证方式": "检查 outputs/测试报告.json 中 test='数据库迁移' 条目的 status 与 detail 字段",
          "判定标准": "status == 'pass' 且 detail 包含 migration 文件和 dev.db 生成信息",
          "期望值": "status = pass，生成 migration 文件 + dev.db"
        },
        {
          "编号": "TEST-3",
          "标准": "测试执行者能执行 tsc --noEmit 并报告编译结果",
          "验证方式": "检查 outputs/测试报告.json 中 test='前端/后端 tsc --noEmit' 条目的 status 字段",
          "判定标准": "status == 'pass' 表示零编译错误，status == 'fail' 表示有编译错误（detail 含错误位置）",
          "期望值": "零编译错误，status = pass"
        },
        {
          "编号": "TEST-4",
          "标准": "测试执行者能执行 npm run build 并验证 dist 目录生成",
          "验证方式": "检查 outputs/测试报告.json 中 test='前端构建' 条目的 status 与 detail 字段",
          "判定标准": "status == 'pass' 且 detail 包含 dist/ 目录生成信息",
          "期望值": "dist/ 目录生成，status = pass"
        },
        {
          "编号": "TEST-5",
          "标准": "测试执行者能启动后端服务并 curl /api/health 验证",
          "验证方式": "检查 outputs/测试报告.json 中 test='后端启动 + Health' 条目的 status 与 detail 字段",
          "判定标准": "status == 'pass' 且 detail 包含 HTTP 响应 {\"status\":\"ok\"}",
          "期望值": "返回 {\"status\":\"ok\"}，status = pass"
        },
        {
          "编号": "TEST-6",
          "标准": "测试执行者能执行核心 API 冒烟测试（CRUD）",
          "验证方式": "检查 outputs/测试报告.json 中 test='核心 API 冒烟' 条目的 status 与 detail 字段",
          "判定标准": "status == 'pass' 且 4 个操作（创建/查询/更新/删除）均 HTTP 200 + 数据正确",
          "期望值": "CRUD 全部 HTTP 200，status = pass"
        },
        {
          "编号": "TEST-7",
          "标准": "测试执行者能执行鉴权验证（不带 token 返回 401）",
          "验证方式": "检查 outputs/测试报告.json 中 test='鉴权验证' 条目的 status 与 detail 字段",
          "判定标准": "status == 'pass' 且 detail 包含 HTTP 401 响应",
          "期望值": "返回 HTTP 401，status = pass"
        },
        {
          "编号": "TEST-8",
          "标准": "7 项测试全部通过时 verdict=confirmed，流转至部署文档管理者",
          "验证方式": "检查 outputs/测试报告.json 的 result.verdict 字段，检查 ROUTER.json 中测试执行者 confirmed 边的目标节点",
          "判定标准": "result.verdict == 'confirmed' 且 confirmed 边指向部署文档管理者",
          "期望值": "verdict = confirmed，路由至部署文档管理者"
        },
        {
          "编号": "TEST-9",
          "标准": "任一测试失败时 verdict=BLOCKING，回退至前端实现者/后端实现者",
          "验证方式": "检查 outputs/测试报告.json 的 result.verdict 字段，检查 ROUTER.json 中测试执行者 BLOCKING 边的目标节点与 max_executions",
          "判定标准": "result.verdict == 'BLOCKING' 且 BLOCKING 边指向前端实现者/后端实现者，max_executions == 3",
          "期望值": "verdict = BLOCKING，路由至前端实现者/后端实现者（根据失败维度定向），max_executions = 3"
        },
        {
          "编号": "TEST-10",
          "标准": "测试执行完成后无残留进程（服务已 kill）",
          "验证方式": "检查测试报告 detail 中是否有进程清理记录，或在测试后执行 ps aux | grep node 验证无残留",
          "判定标准": "无 node 服务进程残留（PORT 3999 端口已释放）",
          "期望值": "零残留进程"
        }
      ],
      "部署执行者验收标准（docker 模式）": [
        {
          "编号": "DEPLOY-1",
          "标准": "[docker] 部署执行者能执行 docker build 并报告镜像构建结果",
          "验证方式": "检查 outputs/部署报告.json 中 build 步骤的 status 字段（deploy_target == 'docker' 时）",
          "判定标准": "status == 'success' 表示镜像构建成功",
          "期望值": "镜像构建成功，status = success"
        },
        {
          "编号": "DEPLOY-2",
          "标准": "[docker] 部署执行者能执行 docker compose up -d 并报告容器状态",
          "验证方式": "检查 outputs/部署报告.json 中 deploy 步骤的 status 字段（deploy_target == 'docker' 时）",
          "判定标准": "status == 'success' 且容器健康运行",
          "期望值": "容器健康运行，status = success"
        },
        {
          "编号": "DEPLOY-3",
          "标准": "[docker] 部署后能 curl /api/health 验证服务可用（30s 超时）",
          "验证方式": "检查 outputs/部署报告.json 中 health_check 步骤的 status 与 detail 字段（deploy_target == 'docker' 时）",
          "判定标准": "status == 'success' 且 detail 包含 {\"status\":\"ok\"}",
          "期望值": "返回 {\"status\":\"ok\"}，30s 内可达"
        },
        {
          "编号": "DEPLOY-4",
          "标准": "[docker] 部署后能执行端到端验证（创建目标 → 分解任务 → 查询统计）",
          "验证方式": "检查 outputs/部署报告.json 中 e2e_validation 步骤的 status 与 detail 字段（deploy_target == 'docker' 时）",
          "判定标准": "status == 'success' 且核心流程全部走通",
          "期望值": "核心流程全部成功，status = success"
        }
      ],
      "部署执行者验收标准（local 模式）": [
        {
          "编号": "LOCAL-DEPLOY-1",
          "标准": "[local] 部署执行者能执行 npm run build 并报告构建结果",
          "验证方式": "检查 outputs/部署报告.json 中 build 步骤的 status 与 detail 字段（deploy_target == 'local' 时）",
          "判定标准": "status == 'success' 且 detail 包含 dist/ 目录生成信息",
          "期望值": "dist/ 目录生成，status = success"
        },
        {
          "编号": "LOCAL-DEPLOY-2",
          "标准": "[local] 部署执行者能通过 node 启动后端服务并报告进程状态",
          "验证方式": "检查 outputs/部署报告.json 中 start_service 步骤的 status 与 detail 字段（deploy_target == 'local' 时）",
          "判定标准": "status == 'success' 且 detail 包含服务监听端口信息（如 PORT 3456）",
          "期望值": "node 服务启动成功，监听指定端口，status = success"
        },
        {
          "编号": "LOCAL-DEPLOY-3",
          "标准": "[local] 部署后能 curl /api/health 验证服务可用（30s 超时）",
          "验证方式": "检查 outputs/部署报告.json 中 health_check 步骤的 status 与 detail 字段（deploy_target == 'local' 时）",
          "判定标准": "status == 'success' 且 detail 包含 {\"status\":\"ok\"}",
          "期望值": "返回 {\"status\":\"ok\"}，30s 内可达"
        },
        {
          "编号": "LOCAL-DEPLOY-4",
          "标准": "[local] 部署后能执行端到端验证（创建目标 → 分解任务 → 查询统计）",
          "验证方式": "检查 outputs/部署报告.json 中 e2e_validation 步骤的 status 与 detail 字段（deploy_target == 'local' 时）",
          "判定标准": "status == 'success' 且核心流程全部走通",
          "期望值": "核心流程全部成功，status = success"
        }
      ],
      "部署执行者通用验收标准（local + docker 共用）": [
        {
          "编号": "DEPLOY-5",
          "标准": "部署成功后 terminal_state = completed（适用于 local 和 docker 两种模式）",
          "验证方式": "检查 STATE.json 中 terminal_state 字段",
          "判定标准": "terminal_state == 'completed'",
          "期望值": "terminal_state = completed"
        },
        {
          "编号": "DEPLOY-6",
          "标准": "部署失败时 verdict=deploy_failed，进入 BLOCKING 状态（适用于 local 和 docker 两种模式）",
          "验证方式": "检查 outputs/部署报告.json 的 result.verdict 字段",
          "判定标准": "result.verdict == 'deploy_failed' 且系统进入 BLOCKING",
          "期望值": "verdict = deploy_failed，BLOCKING 等待用户介入"
        }
      ]
    },

    "七、文档层级": {
      "说明": "文档层级列出文档类型列表、文档间依赖关系、文档层与执行层关系、每层审阅角色。",

      "文档类型列表": [
        {
          "层级": "L0 需求层",
          "文档类型": ["需求文档（00-需求描述.md）"],
          "产出角色": "需求接收者",
          "审阅角色": ["需求接收者（校验）", "需求审计者"],
          "说明": "DAG 起点文档，定义目标、核心能力、角色清单、协作流程、对抗性、验收标准、文档层级七大要素"
        },
        {
          "层级": "L1 设计层",
          "文档类型": [
            "前端需求规格文档",
            "前端交互规范",
            "后端需求规格文档",
            "后端业务逻辑文档",
            "交互逻辑设计文档",
            "美学布局设计文档",
            "接口翻译文档",
            "后端架构设计文档",
            "数据库设计文档",
            "API详细设计文档"
          ],
          "产出角色": ["前端文档管理者", "后端文档管理者", "交互逻辑设计师", "美学布局设计师", "接口翻译师", "后端架构设计师", "数据模型设计师", "API契约设计师"],
          "审阅角色": ["交叉审核者", "综合裁决者"],
          "说明": "文档层对抗由交叉审核者执行，验证前后端文档一致性"
        },
        {
          "层级": "L2 代码层",
          "文档类型": ["前端代码（outputs/src/frontend/）", "后端代码（outputs/src/backend/）", "数据库Schema（outputs/prisma/schema.prisma）", "部署配置（outputs/docker-compose.yml）"],
          "产出角色": ["前端实现者", "后端实现者"],
          "审阅角色": ["前端实现者（校验）", "后端实现者（校验）", "合并校验者", "测试执行者（运行时验证）"],
          "说明": "代码层先经静态校验（合并校验者），再经运行时验证（测试执行者·新增）"
        },
        {
          "层级": "L3 测试报告层（新增）",
          "文档类型": ["测试报告（outputs/测试报告.json）"],
          "产出角色": "测试执行者",
          "审阅角色": [],
          "参考消费角色": ["部署文档管理者（消费测试报告结果编写部署文档，非正式审阅角色）"],
          "说明": "测试报告为 process 类型，记录 7 维度测试结果与 verdict。部署文档管理者作为参考消费角色读取测试报告以编写部署文档，不构成正式审阅关系"
        },
        {
          "层级": "L4 部署层",
          "文档类型": ["部署指南（outputs/部署指南.md）", "运维手册（outputs/运维手册.md）", "变更日志（outputs/CHANGELOG.md）"],
          "产出角色": "部署文档管理者",
          "审阅角色": ["综合裁决者"],
          "说明": "部署文档经综合裁决者审阅，确认后流转至部署执行者"
        },
        {
          "层级": "L5 交付层（新增部署报告）",
          "文档类型": ["最终裁决书（outputs/最终裁决书.md）", "构建报告（outputs/构建报告.md）", "部署报告（outputs/部署报告.json）"],
          "产出角色": ["综合裁决者", "部署执行者"],
          "审阅角色": ["用户（manual 确认）"],
          "说明": "部署报告为 deliverable 类型，记录部署结果与端到端验证，为终态交付物"
        }
      ],

      "文档间依赖关系": [
        "L0 需求文档 → L1 设计文档（需求驱动设计）",
        "L1 前后端文档 → L1 设计文档（交叉审核后驱动详细设计）",
        "L1 设计文档 → L2 代码（设计驱动实现）",
        "L2 代码 → L3 测试报告（代码经测试验证，新增）",
        "L2 代码 + L3 测试报告 → L4 部署文档（代码+测试结果驱动部署文档编写）",
        "L4 部署文档 → L5 最终裁决书（部署文档经终审裁决）",
        "L5 最终裁决书(confirmed) → L5 部署报告（终审通过后执行部署，新增）"
      ],

      "文档层与执行层关系": {
        "说明": "文档层（L0-L1）定义「做什么」，执行层（L2-L5）定义「怎么做+验证+交付」。测试执行者（L3）和部署执行者（L5）为本次新增的执行层验证与交付角色。",
        "层级映射": {
          "文档层": "L0 需求 → L1 设计（经交叉审核对抗）",
          "代码层": "L2 代码（经合并校验静态对抗 + 测试执行者运行时验证）",
          "部署层": "L4 部署文档 → L5 终审裁决 → L5 部署执行（经综合裁决全局对抗 + 用户确认部署）"
        }
      }
    },

    "审计修正记录": {
      "说明": "以下为本轮修正中解决的审计反馈问题，供下游角色追溯。",
      "修正项": [
        {
          "编号": "AUDIT-01",
          "严重程度": "高",
          "问题": "「代码生成者」角色不存在且与上下游描述冲突",
          "修正措施": "删除全部「代码生成者」引用，统一回退目标为「前端实现者/后端实现者」（代码 producer 节点），根据测试失败维度定向回退。涉及位置：角色清单循环回退、DAG变更概要、完整DAG拓扑、迭代循环规则、TEST-9、全局约束第4条",
          "影响范围": ["三、角色清单", "四、协作流程", "六、验收标准 TEST-9", "约束条件"]
        },
        {
          "编号": "AUDIT-02",
          "严重程度": "中",
          "问题": "DATABASE_URL=file:./test.db 与 TEST-2 期望值 dev.db 矛盾",
          "修正措施": "统一 DATABASE_URL 为 file:./dev.db，使 F1 约束条件与 TEST-2 期望值一致",
          "影响范围": ["二、核心能力 F1 约束条件"]
        },
        {
          "编号": "AUDIT-03",
          "严重程度": "中",
          "问题": "local 部署模式声明要实现但验收标准全为 docker",
          "修正措施": "将部署验收标准拆分为三组：docker 模式（DEPLOY-1~4）、local 模式（LOCAL-DEPLOY-1~4）、通用模式（DEPLOY-5~6），确保 local + docker 双模式均有可证伪验收标准",
          "影响范围": ["六、验收标准", "二、核心能力 F2 描述"]
        },
        {
          "编号": "AUDIT-04",
          "严重程度": "低",
          "问题": "DAG中「校验者」与「合并校验者」名称不统一",
          "修正措施": "统一所有「校验者」为「合并校验者」，确保 DAG 拓扑节点命名一致",
          "影响范围": ["三、角色清单 DAG位置", "四、协作流程 DAG变更概要"]
        },
        {
          "编号": "AUDIT-05",
          "严重程度": "低",
          "问题": "L3层将参考消费角色误标为审阅角色",
          "修正措施": "L3 层审阅角色置空，新增「参考消费角色」字段，将部署文档管理者从审阅角色更正为参考消费角色",
          "影响范围": ["七、文档层级 L3 测试报告层"]
        }
      ]
    },

    "约束条件": {
      "全局约束": [
        "1. 改造为增量优化，不删除任何现有角色，不改变现有三层对抗治理结构和前端预览闭环",
        "2. 测试执行者（P0）优先实现，部署执行者（P1）次之，交互式需求澄清（P2）暂不实现",
        "3. 测试执行者需要 Bash 工具，测试在本地沙箱运行",
        "4. 测试执行者回退目标为前端实现者/后端实现者（根据测试失败维度定向回退至对应代码 producer），max_executions: 3",
        "5. 部署执行者为终态角色，confirm: manual，失败无自动回退",
        "6. 部署执行者首期实现 local + docker 双模式，两种模式均需有验收标准覆盖",
        "7. 具体的角色 skill 设计、edge 编排方案、技术选型由下游改造分析师和改造架构师负责，本需求文档仅描述用户意图和改造方向",
        "8. 改造后总角色数预期为 23（21 + 测试执行者 + 部署执行者）"
      ]
    },

    "当前APP结构快照": {
      "说明": "以下为改造分析师/改造架构师参考用的当前结构概览（第二轮改造后的基线），不作为改造需求的约束，仅供下游分析时对照。",
      "改造基线": "第二轮改造结果——21 角色前后端分离架构 + 前端预览闭环",
      "当前manual节点": ["需求接收者(confirm: manual)", "前端美学师(confirm: manual)", "综合裁决者(confirm: manual)"],
      "现有角色数": 21,
      "现有编排特征": "前后端分离 + 3处并行扇出 + 5处同步汇入 + 前端预览闭环 + 运行时验证(后端启动/全栈联调) + 三层对抗 + 定向回退",
      "现有测试机制": "后端启动验证者(后端启动验证) + 全栈联调验证者(前后端联调验证)，但无独立的全量测试执行角色",
      "现有部署机制": "部署文档管理者产出部署指南/运维手册，综合裁决者终审后人工部署，无自动部署角色"
    }
  }
}
