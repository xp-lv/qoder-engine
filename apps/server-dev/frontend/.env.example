# 前端环境变量示例
# 复制为 .env.local 后按需修改

# 后端 API 地址（REST + WS 共用端口 3000，见后端实现报告）
# 开发期 vite.config.ts 已配置 /api 与 /ws 代理到 localhost:3000，可保持相对路径
VITE_API_BASE_URL=http://localhost:3000
VITE_WS_BASE_URL=ws://localhost:3000
